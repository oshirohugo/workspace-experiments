import { appVariable } from "@hoshiro/app";
import { componentVariable } from "@hoshiro/components";
import { coreVariable } from "@hoshiro/core";

export const serverVariable = `serverVariable-${appVariable}-${componentVariable}-${coreVariable}`;
